User-agent: *
Allow: /

# The editor's desk is token-gated and carries a noindex; there is nothing here
# for a crawler to read.
Disallow: /admin

# Query strings on the directory produce the same tools in a different order.
# The pages themselves stay crawlable; only the filtered permutations are not.
Disallow: /*?search=
Disallow: /*?sort=
Disallow: /*?minRating=
Disallow: /*?maxPrice=

# www is the canonical host — the apex 308-redirects to it, so the sitemap is
# declared there. A Sitemap: line that redirects is a needless hop for a crawler.
Sitemap: https://www.toolhaven.net/sitemap.xml
